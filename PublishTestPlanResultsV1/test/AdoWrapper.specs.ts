describe("AdoWrapper", () => {


  // it should fetch project name
  // it should throw error if project name does not exist

  // it should fetch all available test plans
  // it should paginate to get all test plans if needed

  // it should fetch all test configuration objects
  // it should paginiate to get all test plans if needed

  // it should fetch all test points in a testplan
  // it should paginate to fetch all test points
  // it should fetch all test points in a testplan for a given config
  
  // it should create a testrun
  // it should batch update testplan results

})